spring.application.name=lab3_4
# Database configuration read from environment variables
# Example env vars to set: JDBC_DATABASE_URL, JDBC_DATABASE_USERNAME, JDBC_DATABASE_PASSWORD
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/lab3_4}
spring.datasource.username=${JDBC_DATABASE_USERNAME:dino}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:claudinolab3_4}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
